###Another Line Ending Converter
A tool to convert between Unix and Windows/DOS line endings in text files.

```
alec -[d/u] [inputfile] [outputfile]
alec -[d/u] [file_to_convert]
```

Currently only Lf to CrLf implemented.
